package com.restosync.comandas.enums;

/** Roles que determinan permisos y vistas operativas dentro de RestoSync. */
public enum UserRole {
    MESERO,
    COCINERO,
    BARTENDER,
    ADMINISTRADOR
}
